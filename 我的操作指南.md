# 我的操作指南（零基础 · 完整版）

> 目标：做一个**带小屏幕的"蓝牙键鼠盒"**寄给朋友，之后你在家就能远程真控他的 iPhone。
>
> 你需要：一台自己的电脑（Windows 或 Mac 都行）+ 能网购 + 大约一个周末。
>
> 全程**零焊接、零编程基础**，每一步都写了"点什么、怎么点"。卡在哪一步，把报错截图发我。

---

## 先看整体路线（看完心里有数）

```
① 网购物料 → ② 部署云服务器 → ③ 烧录盒子 → ④ 自己先测通 → ⑤ 打包装 APK → ⑥ 寄给朋友
```

- **盒子**（ESP32 + 小屏幕）插在朋友家充电头上，连他家 WiFi、蓝牙连他 iPhone，长年待命
- **你的安卓手机**装咱们的控制 App，通过**云服务器**把指令传到盒子，盒子转成蓝牙键鼠信号控制 iPhone
- **看画面**用微信视频通话的"共享屏幕"（免费，不用开发账号）

---

## ⭐ 准备：先认识给你的这些文件

这个项目里已经帮你写好了所有代码，位置如下（都在 `remote-control` 文件夹里）：

| 文件 | 是什么 | 在哪个阶段用 |
|---|---|---|
| `server/server.js` | 云服务器程序（中转站） | 阶段二，传到服务器 |
| `firmware/esp32-bridge-display/display_bridge.ino` | 盒子固件（带屏幕版） | 阶段三，烧录进板子 |
| `android/` 整个文件夹 | 你的控制 App（安卓） | 阶段六，打包成 APK |
| `朋友配对指南.md` | 打印寄给朋友 | 阶段七 |

> 这些文件现在都在你这个项目的工作区里。你在电脑上操作时，需要把对应文件**下载/导出到本地**：
> 用当前工具的**左侧文件面板**找到 `remote-control` 文件夹，右键需要的文件 → 下载/导出。
> 如果你的工具面板没有"下载"选项、或者不知道怎么把文件弄到电脑，**直接跟我说，我教你**（也可以帮你打包成一个压缩包）。

**给纯新手的建议**：先把整个 `remote-control` 文件夹下载到电脑桌面，后面所有步骤都在这个文件夹里找文件。

---

## 阶段〇：先搞清楚要花多少钱

| 项目 | 价格 | 必须？ |
|---|---|---|
| ESP32-WROOM-32 开发板 ×2 | ¥40-80 | ✅ 盒子本体（1 块调试 + 1 块寄出） |
| 0.96 寸 OLED 屏 SSD1306（4针）×2 | ¥10-20 | ✅ 盒子的小屏幕（显示状态） |
| 母对母杜邦线（一小把） | ¥3 | ✅ 屏幕接板子用，4 根 |
| 数据线（Type-C 或 Micro-USB，能传数据） | ¥15 | ✅ 烧录用，别用纯充电线 |
| 给朋友的充电头 + 线（品牌） | ¥30 | ✅ 盒子在朋友家 7×24 供电 |
| 云服务器（海外 VPS） | ¥50-100/年 | ✅ 远程传递指令必需，买朋友所在国节点 |
| **合计（首年）** | **≈¥140-250** | |

**看画面免费**：用微信/QQ 的"共享屏幕"，**不需要** Apple 开发者账号（¥688/年那个用不上）。

---

## 阶段一：网购（5 分钟下单，等 2-3 天快递）

### 1. 开发板（就是盒子本体，先买它）

**搜索词**：`ESP32 开发板 CP2102`

**怎么认对（铁律）**：商品标题/图片里必须有 **WROOM-32** 五个字，且**没有** C3 / C6 / S2 / S3 / H2 这些字样。

✅ 对的样子：标题类似 *"ESP32开发板 WIFI+蓝牙双核…CP2102/CH340"*，芯片上印 `ESP32-WROOM-32`

❌ 千万别买：ESP32-C6、ESP32-C3、ESP32-S3、ESP32-S2、ESP32-H2（这些都没经典蓝牙或没 WiFi，烧进去跑不起来）

**下单选项**：
- 引脚数选 **38Pin**（推荐，GPIO21/22 都在，接屏幕稳）
- 接口 Type-C 或 Micro-USB 都行
- 选 **"已焊好排针"** 的版本（商品页有选项就选；没有就下单前问客服一句"是焊好排针的吧"）
- 买 **2 块**

### 2. 屏幕（盒子的"脸"）

**搜索词**：`0.96 寸 OLED SSD1306 I2C 4针`

认准 **4 个针脚**（VCC / GND / SCL / SDA）。有些是 SPI 7 针的，接法完全不同，别买错。买 2 块备用。

### 3. 杜邦线

**搜索词**：`母对母 杜邦线`

买一小把（¥3），实际只用 4 根，其余留着。

### 4. 数据线

看板子接口买（Type-C 或 Micro-USB），**商品页必须有"支持数据传输"**。纯充电线没法烧录。

### 5. 给朋友的充电头

品牌 5V/1A USB 充电头（ANKER/绿联 ¥30 左右）。劣质电源会导致盒子 WiFi/蓝牙随机掉线重启——这是 7×24 挂机最常见的隐形坑，别省这个钱。

### 6. 云服务器（买朋友所在国的海外节点，别买国内）

因为朋友在国外，服务器必须放**朋友所在国家**，盒子那段 7×24 长连接才稳（放国内会让盒子跨境常连、动不动掉线）。

**推荐 Vultr**（vultr.com，注册简单、支持支付宝、按小时计费）：
- 朋友在日本 → 选 **东京** 节点
- 朋友在韩国 → 选 **首尔** 节点
- 朋友在新加坡/马来/印尼 → 选 **新加坡** 节点
- 朋友在泰国/越南 → 选 **新加坡** 节点（就近）

配置选 **2核1G 或 2核2G** 最低配、系统 **Ubuntu 22.04**。

> 想国内访问更快，也可看腾讯云国际/阿里云国际的新加坡、香港节点（对国内线路更优），但注册实名稍麻烦，新手先上 Vultr 跑通。

付款后进控制台，**记下两样东西**（后面要用）：
- **公网 IP**（一串数字，如 `123.45.67.89`）
- **root 密码**（你买的时候自己设的）

> 盒子连服务器**不需要梯子**（它在朋友家本地连）；只有你国内手机连海外服务器时开梯子优化线路。

---

## 阶段二：搞定服务器（约 30 分钟）

服务器是一台放云端、24 小时不关机的电脑。你和朋友的盒子都连它，指令就靠它跨城市转发。

### 第 1 步：连上服务器

1. 电脑装 **Termius**（免费，官网 termius.com 下载）
2. 打开 Termius → 点 **+ New Host**：
   - Address 填：公网 IP
   - 用户名：`root`
   - 密码：你买服务器时设的那个
3. 双击连接，第一次会问"是否信任"，选 **Yes**

### 第 2 步：安装运行环境

连上后出现一个黑底命令行窗口。**一条一条**复制下面的命令粘贴进去，每条后面按回车，等它跑完（出现新的 `root@xxx:~#` 提示符）再粘下一条：

```bash
apt update
```

```bash
apt install -y nodejs npm
```

```bash
npm install -g pm2
```

```bash
mkdir -p /root/rc
```

```bash
cd /root/rc && npm init -y && npm install ws
```

### 第 3 步：把服务器程序传上去

1. 在 Termius 里找 **SFTP**（文件传输）按钮（GUI 版在窗口上方/侧边）
2. 把本地 `remote-control/server/server.js` 这个文件拖进服务器的 `/root/rc` 文件夹
3. （`package.json`、`package-lock.json` 传不传都行，不影响）

### 第 4 步：启动并设置开机自启

继续在黑窗口里依次运行：

```bash
cd /root/rc && pm2 start server.js --name rc-relay
```

```bash
pm2 save
```

```bash
pm2 startup
```

最后一条 `pm2 startup` 会打印一大段提示，里面有一行以 `sudo ...` 开头的命令，**把那一整行复制粘贴运行**（这是让它开机自动启动的关键）。

### 第 5 步：放行防火墙端口

回到云服务器官网控制台 → 找 **"防火墙"** → **添加规则**：
- 协议：**TCP**
- 端口：**9000**
- 来源：**所有（0.0.0.0/0）**
- 点确定

### 第 6 步：验证服务器活着

在 Termius 黑窗口运行：

```bash
pm2 list
```

看到 `rc-relay` 状态是 **online** 就成功了。到此服务器永久在线，重启也会自动拉起。

---

## 阶段三：给盒子烧录程序（约 1 小时）

"烧录" = 把 `display_bridge.ino` 这个程序写进开发板。板子和数据线到货后开始。

### 第 1 步：装 Arduino IDE

1. 去 arduino.cc 官网，下载并安装 **Arduino IDE**（免费，一路默认安装）
2. 打开它

### 第 2 步：让 IDE 认识 ESP32 芯片

1. 顶部菜单 **文件 → 首选项（Preferences）**
2. 找到 **"附加开发板管理器网址"** 那一栏，粘贴：

```
https://espressif.github.io/arduino-esp32/package_esp32_index.json
```

3. 点 **好 / OK**
4. 顶部菜单 **工具 → 开发板 → 开发板管理器**，搜索框输入 `esp32`
5. 找到 **"esp32 by Espressif Systems"** → 点 **安装**
   - ⚠ 如果装最新版后后面编译报错，回到这里点该条目 → 选择版本 → 选 **3.2.0** 再装一次

### 第 3 步：装 6 个库

顶部菜单 **工具 → 管理库**，在搜索框依次搜索并安装（认准作者，别装错同名的）：

| 搜索词 | 作者 | 说明 |
|---|---|---|
| `U8g2` | Oliver Kraus | 屏幕驱动，必装 |
| `NimBLE-Arduino` | h2zero | 主库的底层依赖，必装 |
| `Callback` | Tom Stewart | 主库的底层依赖，必装 |
| `WebSockets` | Markus Sattler | 连服务器 |
| `WiFiManager` | tzapu | 配 WiFi |
| `ArduinoJson` | Benoit Blanchon | 解析指令，装 **v6** 版本 |

### 第 4 步：装主库（重点，它不在库里）

1. 浏览器打开：`https://github.com/Mystfit/ESP32-BLE-CompositeHID`
2. 点绿色 **Code** 按钮 → **Download ZIP**（存到桌面，**不要解压**）
3. Arduino IDE 菜单 **项目 → 导入库 → 添加 .ZIP 库** → 选刚下载的 ZIP
4. ⚠ 如果你以前装过 `ESP32-BLE-Keyboard` / `ESP32-BLE-Mouse` / `ESP32-BLE-Combo`，先在"管理库"里**卸载**它们，否则报 `BleConnectionStatus` 重复定义。

### 第 5 步：打开固件并烧录

1. Arduino IDE 菜单 **文件 → 打开** → 选本地 `remote-control/firmware/esp32-bridge-display/display_bridge.ino`
2. **不要改代码**——服务器地址和房间码不在固件里写死，改为配网页面填写（见第 10 步），保证公开仓库里不泄露任何服务器 IP / 房间码。

> 房间号就当**密码**用！别用姓名拼音这种好猜的，用 `FQ7M-K92X-38TQ` 这类长随机串。知道房间号的人才能进你的房间。配置页面已填过的服务器/房间码会在下次配网时**留空沿用上次**，重新输入才覆盖。

### 第 6 步：接上屏幕（免焊，4 根杜邦线）

| 屏幕针脚 | 接到板子 |
|---|---|
| VCC | 3V3 |
| GND | GND |
| SCL | D22（GPIO22） |
| SDA | D21（GPIO21） |

母对母杜邦线一头插屏幕针脚、一头插板子排针，一压就紧，不用焊不用胶。

### 第 7 步：连接板子并设置

1. 数据线一头插板子、一头插电脑
2. 顶部菜单 **工具 → 开发板** → 选 **"ESP32 Dev Module"**
3. **工具 → 端口** → 选新出现的 COM 口（Windows）或 `/dev/cu.usb...`（Mac）

### 第 8 步：设置分区（关键，跳过会编译报错）

1. **工具 → Partition Scheme** → 选 **"Huge APP (3MB No OTA/1MB SPIFFS)"**
2. **工具 → Flash Size** → 选 **"4MB"**

> 原因：中文完整字库约 200KB，加上蓝牙+WiFi 库，默认 1.25MB 分区装不下，会报 `region overflow`。

### 第 9 步：上传（烧录）

点左上角 **→（上传）** 按钮，等底部出现 **"上传成功 / Done uploading"**。

- 失败的话：工具 → Upload Speed 改成 `115200`，再按住板上 BOOT 键上传
- 还失败：换一根数据线（可能买到纯充电线了）

### 第 10 步：验证成功

烧录后：
1. 插电瞬间屏幕先**渐显渐隐**一个大大的 **"MAKE STUDIO"** 开机动画（约 6 秒），再接正常状态显示
2. 屏幕亮起，显示 **"RC 远程助手"** + 四行状态 = 硬件 OK
   （WiFi / 被控端 / 控制端 / 房间号，见下）
3. 顶部菜单 **工具 → 串口监视器**，波特率设 **115200**，看到 `[BLE] RC Bridge 已广播` = 程序跑起来了

> 💡 热门提示：开机动画文字 `MAKE STUDIO` 是全大写、渐显再渐隐，风格统一来自 App/固件同款设计。想改成别的字，到固件里 `runBootAnimation()` 的 `logo` 处改即可，但建议保持全大写。

---

## 阶段四：自测（强烈建议，先用自己手机测通再寄）

别把问题寄给朋友，自己先全链路测通：

### 第 1 步：盒子联网

1. 盒子插到任意 USB 充电头
2. 手机 WiFi 列表找一个 **mochadangao** 热点，点连接（无密码）
3. 手机会自动弹出配置页（不弹就开浏览器访问 `10.10.10.1`）
4. 页面里（居中卡片样式的"RC 远程助手"页）有两个**必须自行填写**的框（这是你手动输入的**全部内容**，见下方表格）：
   - 选**你家 WiFi** → 输密码
   - **服务器地址**：填 `ws://你的服务器公网IP:9000`（IP 就是阶段一所记的那串数字，`ws://` 前缀一定要带上）
   - **房间号**：填一个**自己定的长随机串**当密码，别用姓名拼音（如 `FQ7M-K92X-38TQ`）
   - 输入框下方有小字提示"留空将沿用上次"——**记住这两栏只要填一次**，以后每次打开配置页保持留空 = 沿用上次保存的值，不会被清掉，也不会改掉
   - 点 **Save**
5. 盒子自动重启连上 WiFi，**屏幕 WiFi 行变成 [OK]**

> 🔑 **关键提醒（安全）**：服务器地址 + 房间号就是进你设备的钥匙，**只能靠口口相传/私聊**发给自己和信得过的朋友，**千万不要**写进这个 md 或任何公开发到网上的地方（现在仓库是公开的，写进去就等于公开你的设备钥匙）。

> 下表是你**手动输入的内容全览**——除这两处外，其它代码都是写死的，不用你改：

| 填写位置 | 要填什么 | 格式示例 | 填几次 |
|---|---|---|---|
| 盒子配网页「服务器地址」 | 你的云服务器地址 | `ws://123.45.67.89:9000` | 首次 1 次，之后可留空沿用 |
| 盒子配网页「房间号」 | 你自己定的房间码 | `FQ7M-K92X-38TQ` | 首次 1 次，之后可留空沿用 |
| 安卓 App「服务器地址」 | 同一个服务器地址 | `ws://123.45.67.89:9000` | 首次 1 次，之后可留空沿用 |
| 安卓 App「房间号」 | 同一个房间码 | `FQ7M-K92X-38TQ` | 首次 1 次，之后可留空沿用 |

> 盒子的房间号 和 App 的房间号**必须一模一样**（大小写不敏感），否则连不进一个房间。

### 第 2 步：iPhone（被控端）配对蓝牙

借一个 iPhone（或你自己的苹果设备）：
1. 设置 → 辅助功能 → 触控 → **辅助触控** 打开
2. 辅助触控页往下 → **设备 → 蓝牙设备**
3. 列表里找 **RC Bridge** → 点它 → 显示已连接
4. 此时盒子**屏幕"被控端"行变成 [OK] 已连接**

### 第 3 步：安卓控制端验证

1. 手机装好咱们的 App（阶段六打包），填 `ws://你的公网IP:9000` + 同一个房间号 → 点"连接画面"（这两个框同样**只要填一次**，之后点进去保持留空 = 沿用上次）
2. 勾选 **"桥接盒模式"**
3. 在画面上滑动 → **iPhone 屏幕出现指针并在动 = 全链路成功** 🎉

> 如果指针不动，先看盒子屏幕：WiFi 和"被控端"是否都 [OK]；再看 App 有没有提示"请先连接画面"。

### 屏幕四行怎么读

| 行 | 含义 | 什么时候变 [OK] |
|----|------|----------------|
| WiFi | 盒子联网状态 | 盒子连上你/朋友家 WiFi |
| 被控端 | 朋友的 iPhone 蓝牙连没连上盒子 | 朋友在苹果设置里点"连接" |
| 控制端 | 你在家的安卓 App 有没有连进房间 | 你 App 点了"连接画面"并进入同一房间 |
| 房间号 | 底部小字，当前房间码 | 配置时填写的房间号 |

---

## 阶段五：看他的画面（免费，用微信/QQ 共享屏幕）

看画面不用开发账号，用现成 App 的"共享屏幕"当眼睛、盒子当手：

### 方案①（首推）：微信视频共享屏幕 —— 朋友什么都不用装

1. 你们正常微信视频通话
2. 他在通话界面点 **"共享屏幕"**
3. 你安卓开**分屏**：上半屏微信看画面，下半屏控制 App（勾"桥接盒模式"）
4. 边看边操作，就是完整 ToDesk 体验

> QQ 视频通话也有"共享屏幕"，FaceTime 也行（但要你也有苹果设备看）。

### 方案②（画质更好）：TeamViewer QuickSupport

1. 朋友 App Store 装 **TeamViewer QuickSupport**（免费）
2. 他打开后把屏幕上的 **ID** 告诉你
3. 你安卓装 **TeamViewer** 主程序，输入 ID 连接看屏幕
4. 同样配合控制 App 分屏

> 这套方案**不需要** ¥688 开发者账号，也不需要在朋友手机上装任何自制 App——我们的 iOS 端已移除，全部靠"共享屏幕当眼睛 + 盒子当手"完成。

---

## 阶段六：把 APK 装进你的安卓手机（约 30 分钟）

控制 App 不是应用商店下载的，是咱们自己的项目，得打包成 APK：

### 第 1 步：装 Android Studio

1. 去 developer.android.com 下载 **Android Studio**（免费，安装包很大，耐心等）
2. 安装时一路默认，首次打开如果问 SDK，让它自动下载

### 第 2 步：打开项目

1. Android Studio 首页点 **Open**
2. 选本地 `remote-control/android` 文件夹 → 打开
3. 首次打开会自动下载依赖（右下角有进度条），**等它跑完**（第一次可能 5-10 分钟）
4. 弹出任何 "Gradle Sync" 提示，点默认/确认

### 第 3 步：手机打开 USB 调试

1. 安卓手机：设置 → 关于手机 → 连点 **"版本号"** 7 次，提示"已进入开发者模式"
2. 返回设置 → 开发者选项 → 打开 **"USB 调试"**

### 第 4 步：连电脑并运行

1. 数据线连手机和电脑，手机弹"允许 USB 调试"→ 允许
2. Android Studio 顶部设备下拉框选你的手机 → 点绿色 **▶ Run**
3. App 自动装进手机并打开 ✅

### 第 5 步（可选）：只要 APK 文件

菜单 **Build → Build Bundle(s)/APK(s) → Build APK(s)** → 完成后点右下角 **locate** →
得到 `app-debug.apk` → 发到手机安装（需允许"未知来源"）。

> debug 版 APK 自己用没问题。想给更多人装，以后我再教你签名打包。

---

## 阶段七：寄出（收尾）

寄给朋友的包裹里放：

- [ ] 烧好程序的盒子（ESP32 + 屏幕，最好用个小盒子裹一下）
- [ ] 品牌充电头 + 线（阶段一买的）
- [ ] 打印一份 **《朋友配对指南.md》**

跟他说一句话：**"插上充电器，按纸上的三步弄一次，以后就再也不用管了。"**

---

## 常见问题速查

| 现象 | 原因 / 解决 |
|---|---|
| 串口监视器没输出 | 波特率没设 115200，或端口选错 |
| 上传一直失败 | 换数据线（可能纯充电线）；按 BOOT 键再传 |
| 编译报 `BleConnectionStatus` 重复定义 | 卸载 ESP32-BLE-Keyboard / Mouse / Combo 库 |
| 编译报 NimBLE 相关错误 | 确认装了 NimBLE-Arduino（h2zero）和 Callback（Tom Stewart） |
| 编译报 `region overflowed` | 阶段三第 8 步没设对：Partition Scheme 选 "Huge APP (3MB No OTA)" |
| 屏幕黑屏不亮 | ①VCC 接 3V3 不是 5V ②买的是 I2C 4针不是 SPI 7针 ③SDA/SCL 插反 |
| 屏幕乱码/漏字 | U8g2 库版本太旧，更新到最新版 |
| 盒子 WiFi 显示 [—] | 没连上 WiFi → 拔插重试，或重进 mochadangao 配网 |
| 安卓连上但指针不动 | ①房间号不一致 ②朋友蓝牙没连（屏幕"被控端"[—]）③没点"连接画面" ④屏幕"控制端"仍是[—]说明你 App 没进对房间 |
| 服务器连不上 | 云控制台防火墙 9000 端口没放行 |
| 想换房间号 | 重进 mochadangao 热点改，或重新烧录 |
| 担心别人连进房间 | 房间号当密码：换成 `FQ7M-K92X-38TQ` 长随机串（盒子 + App 同步改） |

---

## 记住三条底线（免得走弯路）

1. **买板子认准 WROOM-32**，其他 C/H/S 系列一概不要。
2. **阶段四自测别跳过** —— 在自己手里把全链路跑通再寄，否则远程 debug 很痛苦。
3. **卡住了就把报错截图发我**，不要自己硬试，我帮你定位。