<?xml version="1.0" encoding="utf-8"?>
<!-- 远程操作页：红框 = 可操作区（按 iPhone 390:844 等比缩放），红点 = 指针定位 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp">

    <!-- 顶栏：左上角断开 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <Button
            android:id="@+id/disconnectButton"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:background="@drawable/bg_btn_ghost"
            android:paddingLeft="16dp"
            android:paddingRight="16dp"
            android:stateListAnimator="@null"
            android:text="断开连接"
            android:textColor="#FFFFFF"
            android:textSize="13sp" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginLeft="10dp"
            android:layout_weight="1"
            android:orientation="vertical">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <ImageView
                    android:id="@+id/statusDot"
                    android:layout_width="10dp"
                    android:layout_height="10dp"
                    android:contentDescription="连接状态"
                    android:src="@drawable/dot_idle" />

                <TextView
                    android:id="@+id/roomChip"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginLeft="6dp"
                    android:text="—"
                    android:textColor="#d8ffffff"
                    android:textSize="11sp"
                    android:textStyle="bold" />
            </LinearLayout>

            <TextView
                android:id="@+id/statusLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="连接服务器中…"
                android:textColor="#FFFFFF"
                android:textSize="13sp"
                android:textStyle="bold" />
        </LinearLayout>

        <Button
            android:id="@+id/btButton"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:background="@drawable/bg_btn_ghost"
            android:paddingLeft="14dp"
            android:paddingRight="14dp"
            android:stateListAnimator="@null"
            android:text="蓝牙"
            android:textColor="#FFFFFF"
            android:textSize="13sp" />
    </LinearLayout>

    <!-- 操作区容器：代码里按 390:844 等比计算红框尺寸并居中 -->
    <FrameLayout
        android:id="@+id/padContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="12dp"
        android:layout_weight="1">

        <FrameLayout
            android:id="@+id/padFrame"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:background="@drawable/bg_pad">

            <View
                android:id="@+id/pointerDot"
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:background="@drawable/pointer_dot"
                android:contentDescription="指针位置" />
        </FrameLayout>

        <!-- 桥接盒不在线时的遮罩：挡住操作并明确告知 -->
        <TextView
            android:id="@+id/offlineOverlay"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:background="@drawable/bg_overlay"
            android:gravity="center"
            android:paddingLeft="24dp"
            android:paddingRight="24dp"
            android:paddingTop="18dp"
            android:paddingBottom="18dp"
            android:text="桥接盒未连接\n\n服务器已连上，但房间里没有小盒子\n无法远程操作 iPhone\n\n盒子通电联网后会自动恢复"
            android:textColor="#FFFFFF"
            android:textSize="13sp"
            android:visibility="gone" />
    </FrameLayout>

    <!-- 手势提示 -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="center"
        android:text="红框内滑动移动指针 · 轻点点击 · 长按拖拽 · 双指滚动"
        android:textColor="#a6ffffff"
        android:textSize="11sp" />

    <!-- 远程打字 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <EditText
            android:id="@+id/textInput"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/bg_input"
            android:hint="远程打字（经盒子键盘键入）"
            android:imeOptions="actionSend"
            android:inputType="text"
            android:padding="12dp"
            android:textColor="@color/ink"
            android:textSize="14sp" />

        <Button
            android:id="@+id/sendTextButton"
            android:layout_width="wrap_content"
            android:layout_height="44dp"
            android:layout_marginLeft="10dp"
            android:background="@drawable/bg_btn_primary"
            android:stateListAnimator="@null"
            android:text="发送"
            android:textColor="#FFFFFF"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- 盒子外设/配置设置入口 -->
    <Button
        android:id="@+id/settingsButton"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/bg_btn_ghost"
        android:stateListAnimator="@null"
        android:text="⚙ 盒子设置（风扇 / 提示音 / 震动 / 房间码）"
        android:textColor="#FFFFFF"
        android:textSize="13sp" />

</LinearLayout>
